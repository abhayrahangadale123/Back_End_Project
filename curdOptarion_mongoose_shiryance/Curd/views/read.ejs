<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link rel="stylesheet" href="../public/Style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <h1 class="font-bold flex justify-center text-3xl m-3">All Users</h1>
    <a class="text-blue-500 m-5 font-bold" href="/Home">Go to Home</a>
<div class="flex flex-wrap">
    <% if(users.length>0){%>
    <% users.forEach((user) => { %>
        
        <div class="flex  mt-5 gap-5">
            <div class="max-w-72 p-4 bg-zinc-200 rounded-lg ml-5 flex flex-col gap-2">
                <div class="w-64 h-72 bg-zinc-500">
                    <!-- Make sure user.urlimg is a valid image URL -->
                    <img src="<%= user.img%>" alt="User Image" class="object-cover w-full h-full">
                </div>
                <h3 class="font-bold"><%= user.name %></h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
                <div class="flex justify-between">
                    <a class="text-zinc-600" href="#">Edit User</a>
                    
                    <form action="/delete/<%= user.id %>" method="POST">
                        <button type="submit" class="text-red-500">Delete</button>
                    </form>
                        
                 </div>
            </div>
        </div>
        <% }) %>
   <% } else{%>
        <h3 class="text-red-500 font-bold text-2xl flex justify-center align-center w-full mt-48">No uers found</h3>
        <%} %>
    </div>

</body>

</html>
